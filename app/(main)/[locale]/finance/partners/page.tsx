import Page from "@/app/(main)/[locale]/(coingarage)/partners/page"

export default function PageFinancePartners({ params: { locale } }: { params: { locale: string } }) {
  return <Page params={{ locale }} />
}
